Question: Is JAVA a Pass-by-value or a Pass-by-reference programming language?
- Java is a Pass-by-value programming language.

Question:
● After the call of swap(jungleDVD, cinderellaDVD), why does the title of these two
objects still remain?
● After the call of changeTitle(jungleDVD, cinderellaDVD.getTitle()) why is
the title of the JungleDVD changed?
- Cause swap function only use local copies and does not affect the original variables outside the main function.
- Despite just a copy, this copy still points to the same object in memory as the original variable. Therefore, when dvd.setTitle() is called, we are directly changing the contents inside that object, so the change will be reflected externally.

Question: 
If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this? 
- Java doesn't allow to do this method because of the same key that mean the DVD(String title) and the 
DVD(String category). They also have the same key "DVD String". However, we can add a new mwthod that DVD int or DVD float 
or match these two String like DVD(String title, String Category) != and DVD(String category, String title).